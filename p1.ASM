 use16	  ;Генерировать 16-битный код
 org 100h  ;Программа начинается с адреса 100h

   mov DI,array ;Стартовый адрес диопазона источника
   mov CX,8    ;Число шагов цикла
   mov AL,0
   mov bp,0000

    ;Цикл по перобору ячеек диопазона

   plus:
     cmp [DI],AL  ;Проверяем если значение DI>=0
     jbe minus	  ;Если условие выполнелось то переходим в minus

     mov bp,[DI]    ;Уменьшить значение ячеки DI на 1
     add bp,1
     mov di,[bp]

     inc DI
     loop plus	  ;CX<-CX-1 , после чего, усли CX<>0, переход на адрес plus


   minus:
 ;    mov DI,[DI-1]
     inc DI	  ;Увеличить значение ячеки DI на 1
     loop plus	  ;CX<-CX-1 , после чего, усли CX<>0, переход на адрес plus


 mov ax,4C00h	;Завершение работы
 int 21h

array  dd 3,-2,5,6,-9,16,-18,88