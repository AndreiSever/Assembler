  ;PDP - 11

  mov #arr,r1	;адрес массива
  mov #5,r2	;кол-во шагов
  clr r3	;Ноль

 plus
   beq hlt	 ;Выход если счетчик r2 = 0
   mov r4,-(r1)  ;Помещаем элемент массива в переменную r4
   cmp r3,r4	 ;сравниваем элемент массива и ноль
   bpl minus	 ;переход усли r3 > r4
   add (r4)+,1	 ;Отнимаем от положительного элемента 1
   mov (r1)+,r4  ;Заменяем элемент массива на новый
   dec r2	 ;Уменьшаем шаг
   br plus	 ;Переход

 minus
   beq hlt	 ;Выход если счетчик r2 = 0
   sub r4,1	 ;Прибавляем к отрицательному элементу 1
   mov (r1)+,r4  ;Заменяем элемент массива на новый
   dec r2	 ;Уменьшаем шаг
   br plus	 ;Переход



 arr: .word 7,-5,2,18,-22